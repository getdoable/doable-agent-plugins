{
  "name": "doable-agent-plugins",
  "version": "0.1.0",
  "private": true,
  "description": "Public, installable agent plugins for Doable.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node scripts/verify-release.mjs",
    "verify": "node scripts/verify-release.mjs"
  }
}
